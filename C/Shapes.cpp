#include <stdio.h>
#include <stdlib.h>

int main() {
	// Here we are just building a shape.
	// For the most part each line statement will read in order,
	// but as we move on we will see how different code will affect
	// how or when each line will be executed.
	printf("   /|\n");
	printf("  / |\n");
	printf(" /  |\n");
	printf("/___|\n");

	system("pause");
	return 0;
}