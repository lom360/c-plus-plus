#include <stdio.h>
#include <stdlib.h>

int main() {

	/*
	 * Commments are ignored by the compiler during execution.
	 * Comments can be helpful when you need to omit code to experiement or test.
	 * Comments are also used to explain lines of code.
	 */

	//printf("Comments are fun");

	system("pause");
	return 0;
}